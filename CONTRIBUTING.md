# Contributing

Работаем через отдельные ветки.

Именование:

- `feature/...`
- `fix/...`
- `docs/...`

Перед merge:

- функциональность запускается локально;
- не ломает обязательные сценарии;
- API-контракт обновлён, если интерфейс backend/frontend изменился;
- секреты и `.env` не коммитятся.
